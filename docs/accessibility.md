# Accessibility notes

- Every form control has a visible label or an explicit accessible name.
- Selection and status are written in text; color is only a supporting signal.
- The progress indicator exposes its value with `role="progressbar"` and ARIA values.
- Bulk changes are announced in a polite live region.
- The app uses native checkboxes, radios, selects, and buttons so keyboard behavior comes for free.

